#include <vector>
#include <map>
#include <string>
#include <cstdio>
#include <iostream>

using namespace std;

int main(){
    vector<vector<int, int> > adj(100);
    int cases, n, m, node;
    cin >> cases;
    for(int x = 0; x < cases; x++){
        cin >> n >> m;
        for(int y = 0; y < cases; y++){
            cin >> node;

        }
    }
}